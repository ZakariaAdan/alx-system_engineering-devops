#!/usr/bin/env bash
# This script displays all active IPv4 IPs on the machine

# Use the `ifconfig` command to list IP addresses and filter out the relevant lines
ifconfig | grep -oP '(?<=inet\s)\d+(\.\d+){3}'
